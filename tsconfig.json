{
  "extends": "./node_modules/gts/tsconfig-google.json",
  "compilerOptions": {
    "jsx": "react",
    "rootDir": ".",
    "outDir": "build",
    // Включает совместимость между CommonJS и ES Modules.
    // Это позволяет использовать синтаксис `import React from 'react'` вместо `import * as React from 'react'`.
    "esModuleInterop": true,
    "lib": [
      "dom",
      "esnext"
    ] // "esnext" для самых последних возможностей ECMAScript
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "test/**/*.ts"
  ]
}